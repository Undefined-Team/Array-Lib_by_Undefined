#include "ud_array.h"
/*
void   ud_arr_transpose_error(ud_arr *arr, ud_arr_size_t_a *new_dim)
{

}

ud_arr *ud_arr_transpose(ud_arr *arr, ud_arr_size_t_a *new_dim)
{
    // check shape == dim & check dim & check if all array have the same type
    ud_arr_size_t_a *shape = ud_arr_get_shape(arr);
    size_t elem_nbr = 1;
    size_t shape_len = shape->len;
    size_t *val = (size_t*)shape->val;
    for (ud_ut_count i = 0; i < len; ++i) elem_nbr *= *val++;
    
}*/